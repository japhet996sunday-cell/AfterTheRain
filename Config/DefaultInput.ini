; Legacy input config kept minimal — this project uses Enhanced Input (InputAction /
; InputMappingContext assets under Content/Input/, referenced by ATRPlayerCharacter)
; as the primary binding system. Populate axis/action mappings here only if a
; third-party plugin still requires the legacy path.
[/Script/Engine.InputSettings]
bAltEnterTogglesFullscreen=True
bUseMouseForTouch=False
DefaultTouchInterface=None
